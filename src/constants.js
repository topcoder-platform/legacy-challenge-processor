/**
 * constants
 */

const prizeSetTypes = {
  Code: 'Code',
  F2F: 'First to Finish',
  CheckPoint: 'Check Point',
  MM: 'Marathon'
}

const phaseTypes = {
  registration: 'registration',
  submission: 'submission',
  checkpoint: 'checkpoint'
}

module.exports = {
  prizeSetTypes,
  phaseTypes
}
